# JWT session signing (must match between Vercel web and Railway auth-service)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=""

# Public API gateway (Railway HTTPS URL in production; local default below)
API_GATEWAY_URL="http://localhost:3003"

# Optional: legacy web monolith DB (cutover / Prisma studio only — runtime uses the gateway)
# DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Optional: Vercel CLI / deploy:web
# VERCEL_TOKEN=""
# VERCEL_PROJECT_NAME="kaizenquest"
# VERCEL_ORG_ID=""
# VERCEL_PROJECT_ID=""

# Optional: Railway CLI / deploy:be
# RAILWAY_TOKEN=""

# --- Local Docker infra (yarn infra:up / yarn dev:complete) ---

AUTH_DATABASE_URL="postgresql://neondb_owner:REDACTED@ep-misty-morning-as4yi777-pooler.c-4.eu-central-1.aws.neon.tech/auth_db?sslmode=require&channel_binding=require"
GOALS_DATABASE_URL="postgresql://neondb_owner:REDACTED@ep-misty-morning-as4yi777-pooler.c-4.eu-central-1.aws.neon.tech/goals_db?sslmode=require&channel_binding=require"
ANALYTICS_DATABASE_URL="postgresql://neondb_owner:REDACTED@ep-misty-morning-as4yi777-pooler.c-4.eu-central-1.aws.neon.tech/analytics_db?sslmode=require&channel_binding=require"
# NOTIFICATIONS_DATABASE_URL="postgresql://kaizen:kaizen@localhost:15436/notifications_db"

KAFKA_BROKERS="localhost:9092"

# Gateway → Nest services (local defaults; on Railway use *.railway.internal)
# AUTH_SERVICE_URL="http://localhost:3001"
# GOALS_SERVICE_URL="http://localhost:3002"
# ANALYTICS_SERVICE_URL="http://localhost:3004"
# WEB_ORIGIN="http://localhost:3000"
