{
  "name": "goals-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/goals-service/src",
  "projectType": "application",
  "tags": ["scope:goals", "type:app"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx watch --tsconfig apps/goals-service/tsconfig.app.json apps/goals-service/src/main.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/build-service.mjs goals-service"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node dist/apps/goals-service/main.mjs"
      },
      "dependsOn": ["build"]
    },
    "prisma-generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma generate --config apps/goals-service/prisma.config.ts"
      }
    },
    "prisma-migrate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/goals-service",
        "command": "prisma migrate dev --config prisma.config.ts --name init"
      }
    },
    "prisma-migrate-deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma migrate deploy --config apps/goals-service/prisma.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/goals-service/tsconfig.app.json"
      }
    }
  }
}
