name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Generate Prisma clients
        run: |
          yarn prisma generate
          yarn prisma generate --config apps/auth-service/prisma.config.ts
          yarn prisma generate --config apps/goals-service/prisma.config.ts
          yarn prisma generate --config apps/analytics-service/prisma.config.ts
        env:
          AUTH_DATABASE_URL: postgresql://kaizen:kaizen@localhost:15433/auth_db
          GOALS_DATABASE_URL: postgresql://kaizen:kaizen@localhost:15434/goals_db
          ANALYTICS_DATABASE_URL: postgresql://kaizen:kaizen@localhost:15435/analytics_db

      - name: Typecheck
        run: yarn typecheck

      - name: Build Nest services
        run: yarn build:services

      - name: Test
        run: yarn test:coverage

      - name: Sonar
        if: env.SONAR_TOKEN != ''
        run: |
          node scripts/disable-sonar-autoscan.mjs
          yarn sonar
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy-be:
    name: Deploy backend to Railway
    needs: validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
      RAILWAY_ENVIRONMENT: ${{ secrets.RAILWAY_ENVIRONMENT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn

      - name: Check Railway token
        run: |
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "::error::Missing RAILWAY_TOKEN GitHub secret."
            exit 1
          fi

      - name: Install Railway CLI
        run: npm i -g @railway/cli

      - name: Deploy Nest services
        run: yarn deploy:be

  deploy-web:
    name: Deploy web to Vercel
    needs: [validate, deploy-be]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_SCOPE: ${{ secrets.VERCEL_SCOPE }}
      API_GATEWAY_URL: ${{ secrets.API_GATEWAY_URL }}
      SESSION_SECRET: ${{ secrets.SESSION_SECRET }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn

      - name: Check secrets
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "::error::Missing VERCEL_TOKEN GitHub secret."
            exit 1
          fi
          if [ -z "$API_GATEWAY_URL" ]; then
            echo "::error::Missing API_GATEWAY_URL GitHub secret."
            exit 1
          fi
          if [ -z "$SESSION_SECRET" ]; then
            echo "::error::Missing SESSION_SECRET GitHub secret."
            exit 1
          fi

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Generate Prisma client
        run: yarn prisma generate

      - name: Link Vercel project
        run: |
          if [ -n "$VERCEL_ORG_ID" ] && [ -n "$VERCEL_PROJECT_ID" ]; then
            echo "Using VERCEL_ORG_ID and VERCEL_PROJECT_ID from secrets"
            exit 0
          fi
          args=(link --repo --yes)
          if [ -n "$VERCEL_SCOPE" ]; then
            args+=(--scope "$VERCEL_SCOPE")
          fi
          yarn vercel "${args[@]}"

      - name: Upsert API_GATEWAY_URL and SESSION_SECRET
        if: env.VERCEL_PROJECT_ID != ''
        run: node scripts/upsert-vercel-gateway-env.mjs

      - name: Pull Vercel environment
        run: yarn vercel pull --yes --environment=production

      - name: Build
        run: yarn vercel build --prod

      - name: Deploy
        run: yarn vercel deploy --prebuilt --prod
